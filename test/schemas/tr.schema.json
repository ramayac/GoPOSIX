{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://goposix.dev/schemas/tr.schema.json",
  "title": "GoPOSIX tr --json output",
  "description": "Validates the complete JSON output of goposix tr --json.",
  "type": "object",
  "required": ["command", "version", "schemaVersion", "exitCode", "data", "error"],
  "properties": {
    "command": { "type": "string", "const": "tr" },
    "version": { "type": "string" },
    "schemaVersion": { "type": "string", "const": "1.0" },
    "exitCode": { "type": "integer" },
    "data": {"type": "object", "required": ["lines", "lineCount", "bytesIn", "bytesOut"], "properties": {"lines": {"type": "array", "items": {"type": "string"}}, "lineCount": {"type": "integer"}, "bytesIn": {"type": "integer"}, "bytesOut": {"type": "integer"}}},
    "error": {
      "oneOf": [
        { "type": "null" },
        {
          "type": "object",
          "required": ["code", "message"],
          "properties": {
            "code": { "type": "string" },
            "message": { "type": "string" }
          }
        }
      ]
    }
  }
}
