{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://goposix.dev/schemas/comm.schema.json",
  "title": "GoPOSIX comm --json output",
  "description": "Validates the complete JSON output of goposix comm --json.",
  "type": "object",
  "required": ["command", "version", "schemaVersion", "exitCode", "data", "error"],
  "properties": {
    "command": { "type": "string", "const": "comm" },
    "version": { "type": "string" },
    "schemaVersion": { "type": "string", "const": "1.0" },
    "exitCode": { "type": "integer" },
    "data": {"type": "object", "required": ["only_file1", "only_file2", "both"], "properties": {"only_file1": {"type": "array", "items": {"type": "string"}}, "only_file2": {"type": "array", "items": {"type": "string"}}, "both": {"type": "array", "items": {"type": "string"}}}},
    "error": {
      "oneOf": [
        { "type": "null" },
        {
          "type": "object",
          "required": ["code", "message"],
          "properties": {
            "code": { "type": "string" },
            "message": { "type": "string" }
          }
        }
      ]
    }
  }
}
